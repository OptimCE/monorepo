# Application Configuration
ALLOWED_ORIGIN=http://localhost:8087

# Frontend Configuration
FRONTEND_API_URL=http://localhost:8087/api
FRONTEND_BASE_URL=http://localhost:8087
FRONTEND_KEYCLOAK_REALM=optimce-realm
FRONTEND_KEYCLOAK_URL=http://localhost:8087/keycloak
FRONTEND_KEYCLOAK_CLIENT_ID=optimce-frontend

# Keycloak secrets
IAM_CLIENT_SECRET=changeme_keycloak_admin_password
KEYCLOAK_ADMIN_PASSWORD=changeme_keycloak_admin_password
KC_BOOTSTRAP_ADMIN_PASSWORD=changeme_keycloak_admin_password

# Database secrets
CRM_DB_PASSWORD=changeme_db_password
KEYCLOAK_DB_PASSWORD=changeme_keycloak_db_password

# Storage (MinIO/S3)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme_minio_password
STORAGE_BUCKET=crm-files
STORAGE_PUBLIC_ENDPOINT=http://localhost:9000
