{
	"apiUrl": "${DOMAIN}:8087${WEB_API_URL}",
	"basePath": "${WEB_BASE_PATH}",
	"keycloak": {
		"realm": "${AUTH_REALM}",
		"url": "${DOMAIN}:8087${WEB_AUTH_URL}",
		"clientId": "${WEB_AUTH_CLIENT_ID}",
		"urlPattern": "^(${DOMAIN}:8087${WEB_API_URL})(/.*)?$",
		"urlPatternFlags": "i"
	}
}
